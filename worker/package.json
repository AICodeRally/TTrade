{
  "name": "ttrade-worker",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply ttrade-db --local"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
